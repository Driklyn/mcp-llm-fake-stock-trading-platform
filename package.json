{
  "name": "mcp-llm-fake-stock-trader",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client",
    "ui",
    "chat-assistant",
    "infra/lambdas/assistant",
    "infra/lambdas/ticks-generator",
    "infra/lambdas/ticks-fetcher",
    "infra/lambdas/trading-api",
    "infra/lambdas/hourly-sync-engine"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "build:assistant": "npm run build --workspace infra/lambdas/assistant",
    "test": "npm test --workspace server && npm test --workspace chat-assistant && npm test --workspace infra/lambdas/assistant",
    "typecheck": "npm run typecheck --workspace client"
  }
}
