{
  "name": "fake-mcp-stock-trader",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client",
    "ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace server"
  }
}
